/// insert material ui theme here - 2023

import { createTheme } from "@mui/material";

export default function theme(){

const theme = createTheme({
    palette: {
      primary: {
        main: '#',
      }
    }
  });
}